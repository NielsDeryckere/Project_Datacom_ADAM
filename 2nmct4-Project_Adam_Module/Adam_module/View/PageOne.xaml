<UserControl x:Class="Adam_module.View.PageOne"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Adam_module.ViewModel"
             xmlns:v="clr-namespace:Adam_module.View"
             xmlns:c="clr-namespace:Adam_module"
             mc:Ignorable="d" d:DesignWidth="960" d:DesignHeight="716" 
             >
    <UserControl.DataContext>
        <vm:PageOneVM/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <c:StateToColorConverter x:Key="StateToColorConverter"/>
        <ControlTemplate x:Key="TemplateDemo" TargetType="Button">
            <Grid>
                <Ellipse Fill="{Binding Ventilator[0], Converter={StaticResource StateToColorConverter} }" Width="50" Height="50"/>

            </Grid>
        </ControlTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>

        </Grid.ColumnDefinitions>
        <Button   Command="{Binding CommandConnect}" Template="{StaticResource TemplateDemo}" Width="50" Height="50"  Grid.Row="0" Grid.Column="0" Margin="33,34,397,35">
            
        </Button>
        <TextBlock Text="{Binding Succeeded}"  TextAlignment="Center" FontSize="24" Width="100" Height="50"  Grid.Row="0" Grid.Column="0" Margin="88,34,292,35" />




    </Grid>
</UserControl>

